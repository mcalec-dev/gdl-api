<div id="settings" class="fixed z-50">
  <div class="fixed bottom-4 left-4 z-50 rounded-xl border border-white/20 p-0 shadow-lg backdrop-blur-md items-center justify-center align-middle">
    <button id="settings-button" class="flex items-center justify-center rounded border-white/20 p-4 font-semibold text-white fill-white stroke-white shadow-2xl backdrop-blur-md transition">
      <span class="w-6 h-6 items-center">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><path d="M12 9h.01" /><path d="M11 12h1v4h1" /><path d="M12 3c7.2 0 9 1.8 9 9s-1.8 9 -9 9s-9 -1.8 -9 -9s1.8 -9 9 -9z" /></svg>
      </span>
    </button>
  </div>
</div>
<div id="settings-container" class="bg-black/20 m-0 fixed left-4 bottom-4 z-50 h-auto w-auto items-center justify-center rounded-xl border border-white/20 p-4 shadow-lg backdrop-blur-md max-w-fit" hidden>
  <h2 id="subtitle" class="mb-4 text-center text-2xl font-light capitalize text-white">
    Settings
  </h2>
  <div class="gap-2 flex-col flex mb-8 align-middle items-center justify-center">
    <div id="oneko-toggle_container" class="flex gap-1 items-center">
      <span class="text-white">
        Show Oneko:
      </span>
      <input class="bg-black/80 rounded border border-white/20 p-0 select-none pointer-events-auto items-center justify-center" type="checkbox" id="oneko-toggle" />
    </div>
    <div id="image-scale-container" class="gap-2 flex-col flex items-center justify-center">
      <span class="text-white">
        Image Scale (default in %):
      </span>
      <div class="flex gap-4 items-center">
        <div class="flex flex-col items-center">
          <span class="text-white/80 text-sm mb-1">min</span>
          <input class="bg-black/80 rounded border border-white/20 text-center px-2 py-1 select-text pointer-events-auto text-white min-w-20" type="number" id="image-scale-min-input" min="10" max="100" step="10" value="10">
        </div>
        <div class="flex flex-col items-center">
          <span class="text-white/80 text-sm mb-1">default</span>
          <input class="bg-black/80 rounded border border-white/20 text-center px-2 py-1 select-text pointer-events-auto text-white min-w-20" type="number" id="image-scale-default-input" min="10" max="200" step="10" value="100">
        </div>
        <div class="flex flex-col items-center">
          <span class="text-white/80 text-sm mb-1">max</span>
          <input class="bg-black/80 rounded border border-white/20 text-center px-2 py-1 select-text pointer-events-auto text-white min-w-20" type="number" id="image-scale-max-input" min="100" max="400" step="10" value="200">
        </div>
      </div>
    </div>
  </div>
  <div id="close-settings" class="fixed bottom-4 left-4 items-start justify-start bg-transparent border-none">
    <button class="select-none w-full h-full text-3xl rounded-full m-0 p-0 text-center text-white">
      ×
    </button>
  </div>
</div>
<script type="module" src="/js/settings.js"></script>
