# Gallery-DL API Configuration

# Server configuration
PORT=3030
BASE_PATH=/gdl

# Path to your gallery-dl downloads directory (can be on another drive)
GALLERY_DL_DIR=F:\gallery-dl

# Comma-separated list of directories to exclude (supports * wildcard)
EXCLUDED_DIRS=.config,*.git,modules,e621,furbooru

# Comma-separated list of files to exclude (supports * wildcard)
EXCLUDED_FILES=.DS_Store,thumbs.db,*.json,urls.txt,

# Comma-separated list of allowed file extensions (including the dot)
ALLOWED_EXTENSIONS=.jpg,.jpeg,.png,.gif,.webp,.mp4,.webm,.mp3,.ogg,.flac,.wav,.zip,.rar,.7z,.tar,.gz,.pdf,.txt,.md,.json,.xml,.csv,.tsv,.xls,.xlsx,.ods,.doc,.docx,.odt,.ppt,.pptx,.odp,.mp3,.flac,.wav,.ogg,.mpeg,.mpg,.mp4

# Maximum directory recursion depth
MAX_DEPTH=15

# Rate limiting configuration
RATE_LIMIT_WINDOW_MINUTES=15
RATE_LIMIT_MAX_REQUESTS=1000